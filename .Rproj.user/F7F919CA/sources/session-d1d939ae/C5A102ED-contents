/*-- scss:defaults --*/

// Choose your favorite sage green hex code for the background:
$body-bg: #e2e8e4;       // Option A: Very soft, muted minimal sage
// $body-bg: #ecefe6;    // Option B: Brighter, slightly lighter linen-sage

// Ensure your text color contrasts beautifully with the green background
$body-color: #2b3a32;    // Deep forest-charcoal for crisp, readable text
$link-color: #4a6b52;    // A richer sage-forest accent for hyperlinks

// Keep the typography clean, elegant, and spaced out (Minimalist)
$font-family-sans-serif: "Inter", "Helvetica Neue", Arial, sans-serif;
$headings-font-weight: 300; // Thin, modern minimalist headers